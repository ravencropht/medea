apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-quota
  namespace: argo-workflows
spec:
  hard:
    requests.cpu: "30"
    limits.cpu: "30"
    requests.memory: 30Gi
    limits.memory: 30Gi
