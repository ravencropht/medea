apiVersion: v1
kind: LimitRange
metadata:
  name: default-resources-limits
  namespace: argo-workflows
spec:
  limits:
    - default:
        cpu: "500m"
        memory: "512Mi"
      defaultRequest:
        cpu: "100m"
        memory: "256Mi"
      type: Container
